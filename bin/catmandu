#!/usr/bin/env perl

use strict;
use warnings;

BEGIN {
    eval "use Catmandu::Cmds" or do {
        use Path::Class;
        use lib file(__FILE__)->dir->parent->subdir('lib')
            ->absolute->resolve->stringify;
        use Catmandu::Cmds;
    };
}

Catmandu::Cmds::run(@ARGV);

