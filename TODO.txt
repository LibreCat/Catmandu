* asynchronous app mode ?
* Terrastore, ElasticSearch Backends
* Validation role
* handle stolen routes in Router::path_for
* pluggable classes / instances so that we can inject behavior
* use Moose duck_type instead of static roles ?
* Index::Solr search with hashref for termquery
* Store::Simple id_field keys
* Store::MongoDB last_error / safe
* make sensible defaults for the command line tools, eg:
    catmandu export data/biblio.db
    catmandu index  data/biblio.db
    catmandu import data/biblio.db < /tmp/marc.export.20100101
* stores: expand requirements to: _id, _revisions, _bucket ?
* use Log::Dispatchouli ?
* utf8
    use utf8
    files binmode utf8
* revisit mounts
